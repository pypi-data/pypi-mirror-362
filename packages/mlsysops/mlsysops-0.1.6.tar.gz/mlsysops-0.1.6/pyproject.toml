[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
markers = [
  "timeout(seconds): per-test timeout (provided by pytest-timeout plugin)"
]
addopts = "-rA --disable-warnings"
