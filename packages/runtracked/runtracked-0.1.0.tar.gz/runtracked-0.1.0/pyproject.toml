[project]
name = "runtracked"
version = "0.1.0"
description = "A terminal-based running tracker with CSV logging and ASCII line charts"
readme = "README.md"
license = {text = "MIT"}
authors = [
  { name = "Eren Öğrül", email = "termapp@pm.me" }
]
requires-python = ">=3.8"
dependencies = []

[project.scripts]
runtracker = "runtracker.cli:main"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"