[tool.pyright]
venvPath = "."
venv = "./.venv"

[tool.uv]
index = [
    { url = "https://mirrors.aliyun.com/pypi/simple" },
]

[build-system]
requires = [
    "setuptools>=42",
    "wheel",
]
build-backend = "setuptools.build_meta"
