[tool.poetry]
name = "dbx-hpexts-bootstrap"
version = "0.4.2"
description = "Bootstrap that installs the private 'common' package from GitHub."
readme = "README.md"
authors = ["francisco javier larios <fjlsoto@gmail.com>"]
packages = [{ include = 'dbx_hpexts_bootstrap'}]

[tool.poetry.dependencies]
python = "^3.10"
packaging = ">=23"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"