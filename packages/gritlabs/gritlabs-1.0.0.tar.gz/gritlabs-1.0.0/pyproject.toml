[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gritlabs"
version = "1.0.0"
description = "A shared language and set of templates for consistent, focused prompt engineering and LLM workflows."
authors = [{name = "GritLabs", email = "gritlabs1@gmail.com"}]
readme = "README.md"
license = {file = "LICENSE"}
requires-python = ">=3.7"
keywords = ["docs", "markdown", "prompt-engineering", "llm"]
