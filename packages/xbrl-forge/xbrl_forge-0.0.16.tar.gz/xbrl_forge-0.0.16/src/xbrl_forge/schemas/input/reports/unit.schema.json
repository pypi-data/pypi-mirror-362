{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://xbrl-forge.org/schema/input/reports/unit",
    "title": "Report Unit",
    "description": "Report Unit representing the reported informations unit",
    "type": "object",
    "required": ["numerator"],
    "properties": {
        "numerator": {
            "description": "The numerator of the unit. For a non-fraction unit please only provide the numerator. The Namespace must be included in the report namespaces object.",
            "$ref": "https://xbrl-forge.org/schema/input/general/xml-element"
        },
        "denominator": {
            "description": "The denominator of the unit. Only provide this for non-fraction units. The Namespace must be included in the report namespaces object.",
            "$ref": "https://xbrl-forge.org/schema/input/general/xml-element"
        }
    }
}