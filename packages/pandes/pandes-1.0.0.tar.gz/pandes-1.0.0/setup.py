from setuptools import setup, find_packages

setup(
    name="pandes",
    version="1.0.0",
    description="ML Lab Programs Runner",
    author="Your Name",
    packages=find_packages(),
    include_package_data=True,
    install_requires=[],
)