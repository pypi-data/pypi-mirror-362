[project]
name = "linkai-aion"
version = "0.1.0"
description = "A lightweight Python utility library for developers."
authors = [
  { name = "Aksel" }
]
license = "MIT" # <--- **Crucial Change:** Use the direct SPDX identifier string
readme = "README.md"
requires-python = ">=3.7"

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"