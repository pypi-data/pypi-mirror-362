[project]
name = "django-cosmograph"
version = "0.2.0"
description = "A django app to get cosmograph visualisations directly in django templates"
authors = [
    { name = "Saranya Balasubramanian", email = "saranya.balasubramanian@oeaw.ac.at" }
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "django>=4.2, <6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
