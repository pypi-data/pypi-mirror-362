[project]
name = "hashlife"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Лёша", email = "pypi@lyosha.org" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
hashlife = "hashlife:main"

[build-system]
requires = ["uv_build>=0.7.21,<0.8"]
build-backend = "uv_build"
