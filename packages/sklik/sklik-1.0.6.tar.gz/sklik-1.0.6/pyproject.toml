[project]
name = "sklik"
version = "1.0.6"
description = "Python Sklik API (unofficial)"
authors = [{ name = "Peter Pleško", email = "peter.plesko@gmail.com" }]
requires-python = "~=3.9"
readme = "README.md"
license = "MIT"
keywords = [
    "sklik",
    "report",
]
dependencies = [
    "pendulum>=3.0.0",
    "pydantic>=2.5.2,<3",
    "requests>=2.31.0,<3",
]

[dependency-groups]
dev = [
    "pandas>=2.3.1",
    "pytest>=7.4.3,<8",
    "python-dotenv>=1.1.1",
    "ruff>=0.9.6",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
