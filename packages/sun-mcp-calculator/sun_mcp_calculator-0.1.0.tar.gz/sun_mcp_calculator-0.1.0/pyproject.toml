[project]
name = "sun_mcp_calculator"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "build>=1.2.2.post1",
    "mcp",
    "twine>=6.1.0",
]

[project.scripts]
sun_mcp_calculato = "sun_mcp_calculato.caculator:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
