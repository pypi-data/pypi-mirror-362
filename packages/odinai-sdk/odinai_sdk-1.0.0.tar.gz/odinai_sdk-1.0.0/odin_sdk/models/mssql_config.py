# coding: utf-8

"""
    API Docs

    API Documentation to interact with

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class MSSQLConfig(BaseModel):
    """
    MSSQLConfig
    """ # noqa: E501
    username: StrictStr = Field(description="Username for the MS-SQL database.")
    password: StrictStr = Field(description="Password for the MS-SQL database.")
    host: StrictStr = Field(description="Host for the MS-SQL database.")
    port: StrictInt = Field(description="Port for the MS-SQL database.")
    database: StrictStr = Field(description="Database name for the MS-SQL database.")
    tables: List[StrictStr] = Field(description="List of tables to be used in the MS-SQL database.")
    always_trust_server: Optional[StrictBool] = None
    __properties: ClassVar[List[str]] = ["username", "password", "host", "port", "database", "tables", "always_trust_server"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of MSSQLConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if always_trust_server (nullable) is None
        # and model_fields_set contains the field
        if self.always_trust_server is None and "always_trust_server" in self.model_fields_set:
            _dict['always_trust_server'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of MSSQLConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "username": obj.get("username"),
            "password": obj.get("password"),
            "host": obj.get("host"),
            "port": obj.get("port"),
            "database": obj.get("database"),
            "tables": obj.get("tables"),
            "always_trust_server": obj.get("always_trust_server")
        })
        return _obj


