[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cwtoken"
version = "0.1.1"
description = "Package handling tokens and URLs"
authors = [{ name = "Connor Crawford" }]
dependencies = ["pandas", "requests"]
requires-python = ">=3.7"
