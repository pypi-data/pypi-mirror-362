[project]
name = "mcp-star-demo"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "kaiming.zhang", email = "kaiming.zhang@transwarp.io" }
]
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.11.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[[tool.uv.index]]
name = "testpypi"
url = "https://test.pypi.org/simple/"
publish-url = "https://test.pypi.org/legacy/"
explicit = true

[project.scripts]
mcp-star-demo = "mcp_star_demo:hello"
