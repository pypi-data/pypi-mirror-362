[project]
name = "wlcore"
version = "0.0.1"
description = "Reserved by WidgetLab team"
authors = [
    { name = "WidgetLab Team", email = "your@email.com" }
]
license = { text = "MIT" }
requires-python = ">=3.8"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
