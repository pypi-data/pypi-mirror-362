[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sidereal_visibility_avg"
version = "2.0.0"
dependencies = []

[project.scripts]
sva = "sidereal_visibility_avg.main:main"