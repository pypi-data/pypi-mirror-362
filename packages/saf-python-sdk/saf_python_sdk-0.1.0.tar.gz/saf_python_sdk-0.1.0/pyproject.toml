[project]
name = "saf-python-sdk"
version = "0.1.0"
description = "Structured Agent Framework for Python to build LLM/AI agents"
authors = [
    { name = "Quanzheng Long", email = "qlong@indeed.com" }
]
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/saf"]