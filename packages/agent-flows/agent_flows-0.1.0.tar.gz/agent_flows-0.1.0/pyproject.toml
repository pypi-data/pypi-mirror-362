[project]
name = "agent-flows"
version = "0.1.0"
description = "An frame work for reasoning"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai>=1.95.1",
]

[project.scripts]
agent-flows = "agent_flows:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
