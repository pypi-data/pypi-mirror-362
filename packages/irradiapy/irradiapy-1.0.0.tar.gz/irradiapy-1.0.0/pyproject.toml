[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "irradiapy"
version = "1.0.0"
description = "This Python package is aimed towards the simulation and analysis of irradiation damage with multiple tools."
readme = "README.md"
requires-python = ">=3.10.0"
license = {text = "MIT"}
authors = [
    {name = "Gutiérrez-Camacho, A.-C.", email = "abel.gutierrez@ua.es"}
]
dependencies = [
    "matplotlib>=3.10.1",
    "numpy>=2.2.4",
    "PyGetWindow>=0.0.9",
    "pywinauto>=0.6.8",
    "scikit_learn>=1.6.0",
    "scipy>=1.14.1",
    "mpi4py>=3.0.0",
]
[tool.setuptools.package-data]
"irradiapy" = ["styles/*.mplstyle"]
