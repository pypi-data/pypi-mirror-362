#!/bin/bash

set -euo pipefail

docker run \
  --rm \
  -v "${PWD}":/experiment \
  -e SKIP_DEPENDENCY_CHECK="" \
  registry.gitlab.com/psynetdev/psynet:v12.1.0 \
  bash -c "cd /experiment && uv lock"
