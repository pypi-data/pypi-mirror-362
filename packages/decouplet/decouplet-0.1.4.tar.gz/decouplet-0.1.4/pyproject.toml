[project]
name = "decouplet"
version = "0.1.4"
description = "Decouple config with support for secrets directory (e.g., Docker secrets)"
authors = [
    { name = "Kulbarakov Bakdoolot", email = "kulbarakovbh@gmail.com" },
]
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "python-decouple>=3.8",
]
