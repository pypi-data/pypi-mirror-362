[project]
name = "linkweaver"
version = "0.5.0"
description = "Extract URLs from markdown files and convert them to individual markdown documents."
readme = "README.md"
authors = [{ name = "David Gasquez", email = "davidgasquez@gmail.com" }]
requires-python = ">=3.13"
dependencies = [
    "markitdown[audio-transcription,pdf,youtube-transcription]>=0.1.2",
]

[project.scripts]
linkweaver = "linkweaver:cli"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
