[tool.poetry]
name = "mseep-langchain-mcp-adapters-example"
version = "1.0.0"
authors = [
    "mseep <support@skydeck.ai>",
]
maintainers = [
    "mseep <support@skydeck.ai>",
]

[tool.poetry.dependencies]
python = ">=3.12,<4.0"

[build-system]
requires = [
    "setuptools>=42",
    "wheel",
]
build-backend = "setuptools.build_meta"
