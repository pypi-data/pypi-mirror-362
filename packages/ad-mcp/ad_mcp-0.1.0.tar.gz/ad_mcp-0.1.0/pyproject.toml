[project]
name = "ad-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.11.0",
]

[project.scripts]
ad-mcp = "ad_mcp:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
