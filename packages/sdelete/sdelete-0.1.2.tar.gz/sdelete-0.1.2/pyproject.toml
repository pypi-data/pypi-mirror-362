[project]
name = "sdelete"
version = "0.1.2"
description = "Terminal-based secure file deletion tool with ncurses UI"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [
    { name="Eren Öğrül", email="termapp@pm.me" }
]
dependencies = []

[project.scripts]
sdelete = "sdelete.cli:main"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"