[project]
name = "layered-persistence"
version = "0.0.2"
authors = [{ name = "Sergey Shkatula", email = "sergey@ceil.dev" }]
description = "Persistence"
keywords = ["persistence", "storage", "layers"]
requires-python = ">=3.7"
license = "MIT"
readme = "README.md"

[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/layered_persistence"]

[project.urls]
Homepage = "https://github.com/ceil-python/persistence"
