[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cuda-pathfinder"
version = "0.0.0"
description = "Temporary stub to reserve project name"
authors = [{ name = "Ralf W. Grosse-Kunstleve" }]
requires-python = ">=3.9"
