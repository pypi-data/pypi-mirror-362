[project]
name = "nunpyyy"
version = "0.1.0"
description = "A package with multiple simple ML programs callable by index."
authors = [{name = "Your Name", email = "your.email@example.com"}]
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.7"

[project.urls]
Homepage = "https://pypi.org/project/nunpyyy"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"