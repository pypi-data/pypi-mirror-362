[project]
name = "pywershell"
version = "0.2.0"
description = "Async PowerShell wrapper for Python"
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name = "genderlessput" }]
requires-python = ">=3.8"
dependencies = [
  "async-property",
  "propcache",
  "loguru",
  "asyncinit"
]