[project]
name = "pywershell"
version = "0.2.3"
description = "Async PowerShell wrapper for Python"
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name = "genderlessput" }]
requires-python = ">=3.8"
dependencies = [
  "async-property",
  "propcache",
  "loguru",
  "asyncinit",
  "gowershell>=0.1.0"
]