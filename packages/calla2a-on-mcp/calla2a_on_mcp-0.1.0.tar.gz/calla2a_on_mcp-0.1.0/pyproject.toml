[project]
name = "calla2a-on-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "a2a-sdk>=0.2.9",
    "click>=8.0.80",
    "mcp>=1.11.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.scripts]
calla2a-on-mcp = "calla2a_on_mcp:main"
calla2a-on-mcp-a2a = "calla2a_on_mcp.run_a2a_server:main"