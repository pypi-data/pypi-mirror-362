name: Batch train

on:
  workflow_dispatch:
  # todo: add cron schedule

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      # todo: get features from the feature store
      # todo: train model
      # todo: check if model is better than the current model
      # todo: save model to the model registry