name: Batch inference

on:
  workflow_dispatch:
  # todo: add cron schedule

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      # todo: get features from the feature store
      # todo: load model from the model registry
      # todo: run batch inference / generate predictions
      # todo: save predictions in the storage layer for downstream services