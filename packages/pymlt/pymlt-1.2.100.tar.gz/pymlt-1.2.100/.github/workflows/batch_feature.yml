name: Batch feature engineering

on:
  workflow_dispatch:
  # todo: add cron schedule
    
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      # todo: fetch raw data from a data source
      # todo: compute ml features
      # todo: save to feature store
