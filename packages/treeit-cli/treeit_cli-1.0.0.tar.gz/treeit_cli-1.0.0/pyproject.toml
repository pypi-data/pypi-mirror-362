[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "treeit-cli"
version = "1.0.0"
description = "CLI tool to generate a directory tree view of your project"
readme = "README.md"
license = {text = "MIT"}
authors = [{name = "Damien Bureau"}]
dependencies = []

[project.scripts]
treeit = "tree_it.cli:main"
