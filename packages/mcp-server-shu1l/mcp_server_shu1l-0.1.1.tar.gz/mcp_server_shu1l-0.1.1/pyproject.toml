[project]
name = "mcp-server-shu1l"
version = "0.1.1"
description = "The MCP server to get local time"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.10.5",
]

[tool.uv.workspace]
members = ["venv"]

[project.scripts]
mcp-server-shu1l = "mcp_server_shu1l.server:main"