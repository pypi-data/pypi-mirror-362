[project]
name = "aaaa-mcpserver4567"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sheng", email = "" }
]
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.11.0",
]

[project.scripts]
mcpserver = "mcpserver:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
