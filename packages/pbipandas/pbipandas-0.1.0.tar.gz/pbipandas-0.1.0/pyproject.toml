[project]
name = "pbipandas"
version = "0.1.0"
description = "Power BI API client"
authors = [{ name = "Your Name", email = "you@example.com" }]
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "requests",
    "pandas"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
