[project]
name = "windermere"
version = "0.1.0"
description = "A python package containing machine learning algorithms, implemented as I learn them."
readme = "README.md"
authors = [
    { name = "Joel Crabbe", email = "joel.crabbe@yahoo.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
windermere = "windermere:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
