from setuptools import setup, find_packages

setup(
    name="NotesMaker",
    version="0.1.0",
    description="Play Musical Notes",
    packages=find_packages(),
    python_requires=">=3.6"
)
