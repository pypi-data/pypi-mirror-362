[project]
name = "mine_task"
version = "1.2.0"
description = "A client library for interacting with the task server."
authors = [
    { name = "Andy Jin", email = "2358155969@qq.com" }
]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "requests>=2.0.0"
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta" 