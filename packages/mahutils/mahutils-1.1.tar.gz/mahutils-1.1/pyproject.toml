[project]
name = "mahutils"
version = "1.1"
description = "myutils : Your complete math solution in Python."
authors = [
  { name="Mahmoud", email="mahmood.alh513@gmail.com" }
]
readme = "README.md"
requires-python = ">=3.7"
license = {text = "MIT"}

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"