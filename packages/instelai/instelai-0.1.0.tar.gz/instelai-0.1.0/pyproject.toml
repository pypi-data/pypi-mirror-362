[project]
name = "instelai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
instelai = "instelai:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
