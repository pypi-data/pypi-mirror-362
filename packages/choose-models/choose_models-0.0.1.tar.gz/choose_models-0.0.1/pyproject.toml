[project]
name = "choose_models"
version = "0.0.1"
description = "Model selection CLI tool"
authors = [{name = "Foresty", email = "dsparthsrivastava@email.com"}]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "numpy",
    "pandas",
    "plotly",
    # aur jo bhi chahiye
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
