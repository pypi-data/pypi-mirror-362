import os
import sys
import serial
# # Open serial port (replace 'COM3' with your port)
# ser = serial.Serial('/dev/cu.usbserial-110', baudrate=921600, timeout=1)

# # Writing data
# ser.write(b'Hello, device!\n')  # Note the 'b' for bytes

# # Reading data
# while True:
#     if ser.in_waiting > 0:
#         data = ser.readline().decode('utf-8').rstrip()
#         print(f"Received: {data}")

# # Close the port when done
# ser.close()

