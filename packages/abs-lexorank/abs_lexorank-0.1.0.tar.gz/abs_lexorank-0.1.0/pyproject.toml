[project]
name = "abs-lexorank"
version = "0.1.0"
description = "A lexorank core library that provide all lexorank related base classes and utilities"
authors = [
    {name = "divy551",email = "divydedakiya5@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
