[project]
name = "veras"
version = "0.1.1"
description = "Universal agent tracing SDK for Python. Drop-in decorators to trace any agent and send to AWS."
authors = [
    { name = "Veras", email = "hi@veras.com" }
]
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "requests>=2.28.0"
]
license = {text = "MIT"}

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta" 