[project]
name = "instel"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.95.1",
]

[project.scripts]
instel = "instel:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
