name: Tests

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-latest]
        python-version: ["3.9", "3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: "latest"

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          uv sync --extra test

      - name: Run tests
        run: |
          uv run pytest tests/

  #   - name: Upload coverage to Codecov
  #     uses: codecov/codecov-action@v4
  #     if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.11'
  #     with:
  #       file: ./coverage.xml
  #       flags: unittests
  #       name: codecov-umbrella

  # lint:
  #   runs-on: ubuntu-latest

  #   steps:
  #   - uses: actions/checkout@v4

  #   - name: Install uv
  #     uses: astral-sh/setup-uv@v3

  #   - name: Set up Python
  #     run: uv python install 3.11

  #   - name: Install dependencies
  #     run: uv sync --dev

  #   - name: Run ruff check
  #     run: uv run ruff check src/

  #   - name: Run ruff format check
  #     run: uv run ruff format --check src/

  #   - name: Run mypy
  #     run: uv run mypy src/

  # benchmark:
  #   runs-on: ubuntu-latest

  #   steps:
  #   - uses: actions/checkout@v4

  #   - name: Install uv
  #     uses: astral-sh/setup-uv@v3

  #   - name: Set up Python
  #     run: uv python install 3.11

  #   - name: Install dependencies
  #     run: uv sync

  #   - name: Run benchmarks
  #     run: uv run python benchmarks/bench_transforms.py

  #   - name: Run examples
  #     run: uv run python examples/basic_transforms.py
