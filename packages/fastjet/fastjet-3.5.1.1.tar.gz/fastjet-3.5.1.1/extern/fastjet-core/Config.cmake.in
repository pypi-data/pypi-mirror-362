@PACKAGE_INIT@

if (@FASTJET_ENABLE_CGAL@)
  find_package(CGAL REQUIRED)
endif()

if (@FASTJET_ENABLE_PLUGIN_SISCONE@)
  find_package(siscone REQUIRED)
endif()

include ( "${CMAKE_CURRENT_LIST_DIR}/fastjetTargets.cmake" )
