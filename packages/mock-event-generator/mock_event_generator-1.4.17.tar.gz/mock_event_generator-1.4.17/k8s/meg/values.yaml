# Default values for MEG.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

meg:
  storageClassName: standard
  image: "containers.ligo.org/emfollow/mock-event-generator:1.4.16"
  gracedb:
    caSecret: gracedb-ca
    hostAliases:
      - ip: 10.100.100.10
        hostnames:
          - gracedb.default.svc.cluster.local
